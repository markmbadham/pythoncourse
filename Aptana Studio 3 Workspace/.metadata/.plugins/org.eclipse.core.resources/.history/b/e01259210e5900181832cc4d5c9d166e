'''
Created on 16 May 2018

@author: mark
'''
import Tkinter as tk

win = tk.Tk()
for row in range(1,4):
    for col in (1,2):
        button = tk.Button(text='{},{}'.format(row,col))
        button.grid(row=row, column=col)

win.mainloop()